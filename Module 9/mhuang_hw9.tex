\documentclass{article}
\linespread{1.3}
\usepackage[margin=50pt]{geometry}
\usepackage{amsmath, amsthm, amssymb, amsthm, tikz, fancyhdr, float, graphicx, spverbatim, systeme}
\pagestyle{fancy}
\renewcommand{\headrulewidth}{0pt}
\newcommand{\changefont}{\fontsize{15}{15}\selectfont}

\fancypagestyle{firstpageheader}{
  \fancyhead[R]{
    \changefont
    \parbox[t]{4cm}{ % Adjust width as needed
      Michael Huang\\
      EN.625.633.81\\
      Homework 9
    }
  }
}

\begin{document}

\thispagestyle{firstpageheader}
{\Large 

\section*{1.}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Exercise4.9.png}
\end{figure}

We start with 
\[
  \int_{-\infty}^{\infty} \exp\{-x^2\} \exp\{\frac{-(x - \mu)^2 y}{2\sigma^2}\} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{-x^2 - \frac{(x - \mu)^2 y}{2\sigma^2}\} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{-x^2 - \frac{(x^2 + \mu^2 - 2x\mu) y}{2\sigma^2}\} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{-x^2 - \frac{x^2y}{2\sigma^2} - \frac{y\mu^2}{2\sigma^2} + \frac{xy\mu}{\sigma^2}\} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{-(1 + \frac{y}{2\sigma^2})x^2 + \frac{xy\mu}{\sigma^2} - \frac{y\mu^2}{2\sigma^2} \} dx
\]
With this expression equating to the form of \(-ax^2 + bx + c\), we can try to complete the square as instructed, which would yield \(-a(x - \frac{b}{2a})^2 + \frac{b^2}{4a} + c\). Let's try to match the format with \(a = 1 + \frac{y}{2\sigma^2}\), \(b = \frac{y\mu}{\sigma^2}\), and \(c = -\frac{y\mu^2}{2\sigma^2}\):
\[
  \frac{b}{2a} = \frac{\frac{y\mu}{\sigma^2}}{2 + \frac{y}{\sigma^2}}
\]
\[
  = \frac{\frac{y\mu}{\sigma^2}}{\frac{2\sigma^2 + y}{\sigma^2}}
\]
\[
  = \frac{y\mu}{2\sigma^2 + y}
\]
and
\[
  \frac{b^2}{4a} = \frac{\frac{y^2\mu^2}{\sigma^4}}{4 + \frac{2y}{\sigma^2}}
\]
\[
  = \frac{\frac{y^2\mu^2}{\sigma^4}}{\frac{4\sigma^2 + 2y}{\sigma^2}}
\]
\[
  = \frac{y^2\mu^2}{4\sigma^4 + 2y\sigma^2}
\]
So plugging in:
\[
  \int_{-\infty}^{\infty} \exp\{ -(1 + \frac{y}{2\sigma^2})(x - \frac{y\mu}{2\sigma^2 + y})^2 + \frac{y^2\mu^2}{4\sigma^4 + 2y\sigma^2} - \frac{y\mu^2}{2\sigma^2} \} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{ -(1 + \frac{y}{2\sigma^2})(x - \frac{y\mu}{2\sigma^2 + y})^2 + \frac{y^2\mu^2 - y\mu^2(2\sigma^2 + y)}{2\sigma^2(2\sigma^2 + y)} \} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{ -(1 + \frac{y}{2\sigma^2})(x - \frac{y\mu}{2\sigma^2 + y})^2 + \frac{y^2\mu^2 - 2y\mu^2\sigma^2 - y^2\mu^2}{2\sigma^2(2\sigma^2 + y)} \} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{ -(1 + \frac{y}{2\sigma^2})(x - \frac{y\mu}{2\sigma^2 + y})^2 + \frac{- 2y\mu^2\sigma^2}{2\sigma^2(2\sigma^2 + y)} \} dx
\]
\[
  = \int_{-\infty}^{\infty} \exp\{ -(1 + \frac{y}{2\sigma^2})(x - \frac{y\mu}{2\sigma^2 + y})^2 + \frac{-y\mu^2}{2\sigma^2 + y} \} dx
\]
\[
  = \exp\{\frac{-y\mu^2}{2\sigma^2 + y}\} \int_{-\infty}^{\infty} \exp\{ -(1 + \frac{y}{2\sigma^2})(x - \frac{y\mu}{2\sigma^2 + y})^2 \} dx
\]
The integral is the Gaussian integral, with the form \(\int_{-\infty}^{\infty} e^{-ax^2} dx = \sqrt{\frac{\pi}{a}}\). Using this:
\[
  = \exp\{\frac{-y\mu^2}{2\sigma^2 + y}\} \sqrt{\frac{\pi}{1 + \frac{y}{2\sigma^2}}}
\]
\[
  = \exp\{\frac{-y\mu^2}{2\sigma^2 + y}\} \sqrt{\frac{\pi}{\frac{2\sigma^2 + y}{2\sigma^2}}}
\]
\[
  = \exp\{\frac{-y\mu^2}{2\sigma^2 + y}\} \sqrt{\frac{2\pi\sigma^2}{2\sigma^2 + y}}
\]
With the additional term in the corrected version of the problem:
\[
  = \frac{1}{\sqrt{\frac{2\pi\sigma^2}{y}}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\} \sqrt{\frac{2\pi\sigma^2}{2\sigma^2 + y}}
\]
\[
  = \sqrt{\frac{y}{2\pi\sigma^2}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\} \sqrt{\frac{2\pi\sigma^2}{2\sigma^2 + y}}
\]
\[
  = \sqrt{\frac{y}{2\pi\sigma^2} \cdot \frac{2\pi\sigma^2}{2\sigma^2 + y}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\}
\]
\[
  = \sqrt{\frac{y}{2\sigma^2 + y}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\}
\]
\[
  = \sqrt{\frac{y}{2\sigma^2 + y}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\}
\]
\[
  = \frac{1}{\sqrt{\frac{2\sigma^2 + y}{y}}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\}
\]
\[
  = \frac{1}{\sqrt{\frac{2\sigma^2}{y} + 1}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\}
\]
\[
  = \frac{1}{\sqrt{\frac{2\sigma^2}{y} + 1}}\exp\{\frac{-y\mu^2}{2\sigma^2 + y}\}
\]
\[
  = \frac{1}{\sqrt{\frac{2\sigma^2}{y} + 1}}\exp\{-\frac{\mu^2}{\frac{2\sigma^2 + y}{y}}\}
\]
\[
  = \boxed{\frac{1}{\sqrt{\frac{2\sigma^2}{y} + 1}}\exp\{-\frac{\mu^2}{1 + \frac{2\sigma^2}{y}}\}}
\]
exactly as we sought to show in the corrected version of the problem.

}
{\Large 

\section*{2.}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Exercise4.10.png}
\end{figure}

We start with considering the accept-reject method for the target desntiy \(f\) based on the instrumental distribution \(g\). 

\subsection*{(a)} 

We aim to show that the Accept-Reject sample \((X_1, \dots, X_n)\) can be associated with two iid samples, \((U_1, \dots, U_N)\) and \((Y_1, \dots, Y_N)\) with corresponding distributions \(\text{Unif}_{[0,1]}\) and \(g\), where \(N\) is the stopping time associated with the acceptance of \(n\) variables \(Y_j\). \\ \\
For generating samples for a target \(f\) using \(g\), given we determine some value \(M\) such that \(M \cdot g(x) \geq f(x)\) for all \(x\), we know that the accept-reject method consists of the following steps: \\
- Generate \(Y \sim g\) \\
- Generate \(U \sim \text{Unif}_{[0, 1]}\) \\
- If \(M \cdot U \leq \frac{f(Y)}{g(Y)}\), accept \(Y\) as a draw from \(f(x)\). \\
So from looking at this, our final sample \((X_1, \dots, X_n)\), by definition, we are repeating this process of generating the initial \(Y_i\) and \(U_i\) \(N\) times, which is when we stop generating. Additionally, we see that by definition, we are looking to stop only when we accept \(n\) values of \(X_i\), so we see that by definition, we can see that the sequence of \(n\) accepted values of \(X\) can indeed by associated with those independently drawn series of \(N\) \(U\) and \(Y\) values, as we sought to show.

\subsection*{(b)}

We aim to deduce that the corresponding estimator of \(E_f[h(X)]\) based on \((X_1, \dots, X_n)\) can be written as 
\[
  \delta_1 = \frac{1}{n} \sum_{i=1}^{n} h(X_i) = \frac{1}{n} \sum_{j=1}^{N} h(Y_j) I_{U_j \leq w_j}
\]
with \(w_j = \frac{f(Y_j)}{Mg(Y_j)}\). \\ \\
We start with the standard estimator \(\frac{1}{n} \sum_{i=1}^{n} h(X_i)\). We know that the \(X\) values in this expectation are the same as those \(Y\) values that we decided to accept in the accept-reject algorithm steps. The indicator for \(U_j \leq w_j\), with the definition of \(w_j\), is literally the same as the criteria that is used to determine whether we accept the \(Y\) value as part of the sample or not; i.e. we are taking the value and multiplying by 1 as part of our estimator if it fulfills the condition or multiplying by 0 and discarding it as part of our estimator, but still taking the sum of all values up to all \(N\) values, which was the number of values that we generated. So we took the equivalent values, but just have excess terms in the summation since we're incorporating all the rejected values but zeroing them out. Finally, we divide by \(n\) to make this a true estimator and take the average of all the accepted values as we did in the first form. These two forms are therefore equivalent.

}
{\Large 

\section*{3.}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Exercise4.15.png}
\end{figure}

We aim to generate random variables \(X_i\) and \(Y_i\) and compare the empirical average and Rao-Blackwellized estimator of \(E_f(X)\) and \(\text{Var}_f(X)\):

\subsection*{(b)} 

\(X | y \sim N(0, y), Y \sim \text{Gamma}(a, b)\), i.e. \(X\) is a generalized \(t\):
\begin{spverbatim}
> nmbSimulations = 1000
> n = 1000
> a = 5
> b = 1
> 
> bruteForce_3b = function(nmbSimulations, n, a, b) {
+   
+   # Need to keep mean and mean^2 for calculating variance
+   means = rep(0, nmbSimulations)
+   means_squared = rep(0, nmbSimulations)
+   
+   for (i in 1:nmbSimulations) {
+     
+     # Simulate n values of Y from Gamma(a,b)
+     y = rgamma(n, shape = a, scale = b)
+     
+     # For each Y value, simulate X|Y ~ N(0, y)
+     x = rnorm(n, mean = 0, sd = sqrt(y))
+     
+     means[i] = mean(x)
+     means_squared[i] = mean(x^2)
+   }
+   
+   variance = mean(means_squared) - (mean(means))^2
+   
+   print(paste("Empirical results with", 
+               "nmbSimulations =", nmbSimulations,
+               ", n =", n,
+               ", a =", a,
+               ", b =", b
+               ))
+   print(paste("mean =", mean(means), "and variance =", variance))
+ }
> 
> rao_blackwell_3b = function(nmbSimulations, n, a, b) {
+   
+   # Need to keep mean and mean^2 for calculating variance
+   means = rep(0, nmbSimulations)
+   means_squared = rep(0, nmbSimulations)
+   
+   for (i in 1:nmbSimulations) {
+     
+     # Simulate n values of Y from Gamma(a,b)
+     y = rgamma(n, shape = a, scale = b)
+     
+     # Calculate conditional expectation E(X|Y)
+     # Since X|Y ~ N(0, y), we have E(X|Y) = 0
+     condExpectation = rep(0, n)
+     
+     means[i] = mean(condExpectation)
+     means_squared[i] = mean(condExpectation^2)
+   }
+   
+   variance = mean(means_squared) - (mean(means))^2
+   
+   print(paste("Rao-Blackwellized results with", 
+               "nmbSimulations =", nmbSimulations,
+               ", n =", n,
+               ", a =", a,
+               ", b =", b
+   ))
+   print(paste("mean =", mean(means), "and variance =", variance))
+ }
> 
> bruteForce_3b(nmbSimulations, n, a, b)
[1] "Empirical results with nmbSimulations = 1000 , n = 1000 , a = 5 , b = 1"
[1] "mean = 0.00101562111045003 and variance = 5.01758577452203"
> rao_blackwell_3b(nmbSimulations, n, a, b)
[1] "Rao-Blackwellized results with nmbSimulations = 1000 , n = 1000 , a = 5 , b = 1"
[1] "mean = 0 and variance = 0"
\end{spverbatim}
We see that the variance is set to 0 in the Rao-Blackwellized estimator. The variability in the variable generation was completely removed by using the conditional expectation instead of actually simulating X values from the normal distribution, which introduces actual variance.

\subsection*{(c)}

\(X | y \sim Bin(y), Y \sim \text{Beta}(a, b)\), i.e. \(X\) is a beta-binomial \(t\):
\begin{spverbatim}
asdf> nmbSimulations = 1000
> n = 1000
> m = 10
> a = 2
> b = 2
> 
> bruteForce_3c = function(nmbSimulations, n, m, a, b) {
+   
+   # Need to keep mean and mean^2 for calculating variance
+   means = rep(0, nmbSimulations)
+   means_squared = rep(0, nmbSimulations)
+   
+   for (i in 1:nmbSimulations) {
+     
+     # Simulate n values of Y from Beta(a,b)
+     y = rbeta(n, shape1 = a, shape2 = b);
+     
+     # For each Y value, simulate X|Y ~ Binomial(m, y)
+     x = rbinom(n, size = m, prob = y);
+     
+     means[i] = mean(x)
+     means_squared[i] = mean(x^2)
+   }
+   
+   variance = mean(means_squared) - (mean(means))^2
+   
+   print(paste("Empirical results with", 
+               "nmbSimulations =", nmbSimulations,
+               ", n =", n,
+               ", m =", m,
+               ", a =", a,
+               ", b =", b
+   ))
+   print(paste("mean =", mean(means), "and variance =", variance))
+ }
> 
> rao_blackwell_3c = function(nmbSimulations, n, m, a, b) {
+   
+   # Need to keep mean and mean^2 for calculating variance
+   means = rep(0, nmbSimulations)
+   means_squared = rep(0, nmbSimulations)
+   
+   for (i in 1:nmbSimulations) {
+     
+     # Simulate n values of Y from Beta(a,b)
+     y = rbeta(n, shape1 = a, shape2 = b);    
+     
+     # Calculate conditional expectation E(X|Y)
+     # Since X|Y ~ Binomial(m, y), we have E(X|Y) = m * y
+     condExpectation = m * y;
+     
+     means[i] = mean(condExpectation)
+     means_squared[i] = mean(condExpectation^2)
+   }
+ 
+   variance = mean(means_squared) - (mean(means))^2
+   
+   print(paste("Rao-Blackwellized results with", 
+               "nmbSimulations =", nmbSimulations,
+               ", n =", n,
+               ", m =", m,
+               ", a =", a,
+               ", b =", b
+   ))
+   print(paste("mean =", mean(means), "and variance =", variance))
+ }
> 
> bruteForce_3c(nmbSimulations, n, m, a, b)
[1] "Empirical results with nmbSimulations = 1000 , n = 1000 , m = 10 , a = 2 , b = 2"
[1] "mean = 5.001012 and variance = 6.998882975856"
> rao_blackwell_3c(nmbSimulations, n, m, a, b)
[1] "Rao-Blackwellized results with nmbSimulations = 1000 , n = 1000 , m = 10 , a = 2 , b = 2"
[1] "mean = 5.00453563874706 and variance = 4.99710120693035"
\end{spverbatim}
We see that the variance is reduced by a significant amount (\(\frac{2}{7} = ~28\%\) or so).

}
{\Large 

\section*{4.}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{Exercise4.18.png}
\end{figure}

For each of the distributions, we examine the variance reductions of only the mean and median, where we are calculating \(E(X)\) and the median. The strategy described is to use the inverse transform method and generate pairs of samples. So given \(f(x)\): \\
- Generate \(U_1, U_2, \dots, U_n \sim \text{Unif}(0, 1)\) \\
- Generate \(X_j = F^{-1}_X(U_j)\) \\
- Generate \(Y_j = F^{-1}_X(1 - U_j)\) \\
which in the text is mentioned to work as \(1-U_j\) is also uniform so \(Y\) still follow sthe distribution function of interest. \\ \\ 
This is the general framework of the R method I'm using for this, with the different inverse CDFs passed in and their outputs pasted below each respective section:
\begin{spverbatim}
> generate_antithetic_comparison = function(nmbSimulations, n, inverse_cdf) {
+   means_X = rep(0, nmbSimulations)
+   medians_X = rep(0, nmbSimulations)
+   means_XY = rep(0, nmbSimulations)
+   medians_XY = rep(0, nmbSimulations)
+   
+   for (i in 1:nmbSimulations) {
+     U = runif(n, 0, 1)
+     X = inverse_cdf(U)
+     Y = inverse_cdf(1 - U)
+     XY = (X + Y) / 2
+     
+     # Using X only
+     means_X[i] = mean(X)
+     medians_X[i] = median(X)
+       
+     # Using both X and Y
+     means_XY[i] = mean(XY)
+     medians_XY[i] = median(XY)
+   }
+   
+   print(paste("Results for", deparse(substitute(inverse_cdf)), ":"))
+   print("Means:")
+   print(paste("Average of means with X only:", mean(means_X)))
+   print(paste("Variance of means with X only:", var(means_X)))
+   print(paste("Average of means with X and Y:", mean(means_XY)))
+   print(paste("Variance of means with X and Y:", var(means_XY)))
+   print("Medians:")
+   print(paste("Average of medians with X only:", mean(medians_X)))
+   print(paste("Variance of medians with X only:", var(medians_X)))
+   print(paste("Average of medians with X and Y:", mean(medians_XY)))
+   print(paste("Variance of medians with X and Y:", var(medians_XY)))
+ }
\end{spverbatim}

\subsection*{(i)} 

For the distribution \(f_1(x) = \frac{1}{\pi(1+x^2)}\):
\[
  F(x) = \int_{-\infty}^{x} \frac{1}{\pi(1+x^2)} dt
\]
As \(\int \frac{1}{\pi(1+x^2)} = \arctan(x)\),
\[
  = \frac{1}{\pi} \arctan(t) |_{-\infty}^{x}
\]
\[
  = \frac{1}{\pi} (\arctan(x) - \arctan(-\infty))
\]
\[
  = \frac{1}{\pi} (\arctan(x) - (-\frac{\pi}{2}))
\]
\[
  F(x) = \frac{\arctan(x)}{\pi} + \frac{1}{2}
\]
And now inverting:
\[
  u = \frac{\arctan(x)}{\pi} + \frac{1}{2}
\]
\[
  u - \frac{1}{2} = \frac{\arctan(x)}{\pi}
\]
\[
  \pi(u - \frac{1}{2}) = \arctan(x)
\]
\[
  x = \tan(\pi(u - \frac{1}{2}))
\]
which is our inverse CDF. Let's now move on to generation:
\begin{spverbatim}
> inverse_cdf_f1 <- function(u) {
+   tan(pi * (u - 0.5))
+ }
> 
> generate_antithetic_comparison(nmbSimulations, n, inverse_cdf_f1)
[1] "Results for inverse_cdf_f1 :"
[1] "Means:"
[1] "Average of means with X only: 1.73679111959869"
[1] "Variance of means with X only: 1379.81535900746"
[1] "Average of means with X and Y: 0"
[1] "Variance of means with X and Y: 0"
[1] "Medians:"
[1] "Average of medians with X only: 0.00116540319035601"
[1] "Variance of medians with X only: 0.00238900258474329"
[1] "Average of medians with X and Y: 0"
[1] "Variance of medians with X and Y: 0"
\end{spverbatim}
We see that the mean is not as close to 0, and with a much larger variance as compared to with the antithetic variables, which have the variance and mean as exactly 0.

\subsection*{(ii)}

For the distribution \(f_2(x) = \frac{1}{2}e^{-|x|}\): \\
- Case 1: For \(x < 0\), \(f_2(x) = \frac{1}{2}e^{x}\) 
\[
  F(x) = \int_{-\infty}^{x} \frac{1}{2}e^{t} dt
\]
\[
  = \frac{1}{2}e^{t} |_{-\infty}^{x}
\]
\[
  = \frac{1}{2}e^{x} - \frac{1}{2}e^{-\infty}
\]
\[
  F(x) = \frac{1}{2}e^{x}
\]
And now inverting:
\[
  u = \frac{1}{2}e^{x}
\]
\[
  2u = e^{x}
\]
\[
  x = \ln(2u)
\]
which matches for \(u < \frac{1}{2}\). \\
- Case 2: For \(x \geq 0\), \(f_2(x) = \frac{1}{2}e^{-x}\)
\[
  F(x) = \int_{-\infty}^{0} \frac{1}{2}e^{t} dt + \int_{0}^{x} \frac{1}{2}e^{-t} dt
\]
\[
  = \frac{1}{2}e^{t} |_{-\infty}^{0} + \frac{1}{2}e^{-t} |_{0}^{x}
\]
\[
  = \frac{1}{2}e^{0} - \frac{1}{2}e^{-\infty} - \frac{1}{2}e^{-x} - (-\frac{1}{2}e^{0}) 
\]
\[
  = \frac{1}{2} - 0 - \frac{1}{2}e^{-x} + \frac{1}{2}
\]
\[
  F(x) = 1 - \frac{1}{2}e^{-x}
\]
And now inverting:
\[
  u = 1 - \frac{1}{2}e^{-x}
\]
\[
  u - 1 = - \frac{1}{2}e^{-x}
\]
\[
  1 - u = \frac{1}{2}e^{-x}
\]
\[
  2 - 2u = e^{-x}
\]
\[
  \ln(2 - 2u) = -x
\]
\[
  x = -\ln(2 - 2u)
\]
which matches for \(u > \frac{1}{2}\).
With our inverse CDFs, let's now move on to generation:
\begin{spverbatim}
> inverse_cdf_f2 <- function(u) {
+   ifelse(u < 0.5, log(2 * u), -log(2 - (2 * u)))
+ }
> 
> generate_antithetic_comparison(nmbSimulations, n, inverse_cdf_f2)
[1] "Results for inverse_cdf_f2 :"
[1] "Means:"
[1] "Average of means with X only: 0.00177025055115529"
[1] "Variance of means with X only: 0.00204950708494339"
[1] "Average of means with X and Y: 0"
[1] "Variance of means with X and Y: 0"
[1] "Medians:"
[1] "Average of medians with X only: 0.00135299494178831"
[1] "Variance of medians with X only: 0.00104570134793778"
[1] "Average of medians with X and Y: 0"
[1] "Variance of medians with X and Y: 0"
\end{spverbatim}
Here as well, we see that the average and variance of both means and medians are reduced completely to 0 as well, but the difference isn't as large here.

\subsection*{(iv)}

For the distribution \(f_4(x) = \frac{2}{\pi\sqrt{3}}(1 + \frac{x^2}{3})^{-2}\), we notice that the distribution itself is in the form of the student's t-distribution \(f(x) = \frac{\Gamma (\frac{\nu + 1}{2})}{\sqrt{\nu \pi} \Gamma (\frac{\nu}{2})} (1 + \frac{x^2}{\nu})^{-\frac{\nu + 1}{2}} \), for which we notice that degrees of freedom \(\nu = 3\). So we have
\[
  f(x) = \frac{\Gamma (\frac{3 + 1}{2})}{\sqrt{3 \pi} \Gamma (\frac{3}{2})} (1 + \frac{x^2}{3})^{-\frac{3 + 1}{2}}
\]
\[
  f(x) = \frac{1}{\sqrt{3 \pi} \frac{\sqrt{\pi}}{2}} (1 + \frac{x^2}{3})^{-2}
\]
\[
  f(x) = \frac{2}{\pi\sqrt{3}} (1 + \frac{x^2}{3})^{-2}
\]
which exactly matches up! We could look up the CDF and take the inverse, but I prefer to let R take care of this using qt(p, df = 3) to take the inverse CDF given the input value \(p\). With our inverse CDF, let's now generate values:
\begin{spverbatim}
> inverse_cdf_f4 <- function(u) {
+   qt(u, df = 3)
+ }
> 
> generate_antithetic_comparison(nmbSimulations, n, inverse_cdf_f4)
[1] "Results for inverse_cdf_f4 :"
[1] "Means:"
[1] "Average of means with X only: -0.00318363452736336"
[1] "Variance of means with X only: 0.00295726086143605"
[1] "Average of means with X and Y: 0"
[1] "Variance of means with X and Y: 0"
[1] "Medians:"
[1] "Average of medians with X only: -0.00174285915858053"
[1] "Variance of medians with X only: 0.00180608465300137"
[1] "Average of medians with X and Y: 0"
[1] "Variance of medians with X and Y: 0"
\end{spverbatim}
Again, we see that the variance of the means and medians were reduced to 0, with slightly greater effect than \(f_2\) but significantly lower than \(f_1\).

}
{\Large 

\section*{5.}

Assuming that we use antithetic variables to estimate some parameter of the standard normal distribution, we must prove that the covariance between \(X_i\) and \(Y_i\) is -1, i.e. \(\text{Cov}(X_i, Y_i) = -1\). \\ \\
We set up with \(U \sim \text{Unif}(0, 1)\). Using the hint with the normal distribution being symmetric about 0, we know that \(\Phi(z) = 1 - \Phi(-z)\) for the range of \(U\), and equivalently that 
\[
  x = \Phi(z) = 1 - \Phi(-z) 
\]
\[
  \Phi(-z) = 1 - x
\]
\[
  -z = \Phi^{-1}(1 - x)
\]
\[
  -\Phi^{-1}(-x) = \Phi^{-1}(1 - x)
\]
So taking advantage of this, let's set \(X = \Phi^{-1}(U)\) and \(Y = \Phi^{-1}(1 - U)\) accordingly. We can then determine that 
\[
  Y = \Phi^{-1}(1 - U) = -\Phi^{-1}(-U) = -X
\]
So therefore 
\[
  \text{Cov}(X_i, Y_i) = \text{Cov}(X_i, -X_i) = -\text{Cov}(X_i, X_i) 
\]
\[
  = -\text{Var}(X_i) = \boxed{-1}
\]
just as we aimed to prove.

}

\end{document}